class Contact {
  final String _id, _name, _mail, _phone;

  Contact(this._id, this._name, this._mail, this._phone);

  get id => _id;
  get name => _name;
  get mail => _mail;
  get number => _phone;
}
